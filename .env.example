# Optional — Helius RPC key for holder counts (free tier at https://helius.dev).
# Without this, price / market cap / volume / 24h change still work via DexScreener;
# holder counts will show "—".
HELIUS_API_KEY=

# Public site URL used for canonical metadata. Defaults to localhost.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
